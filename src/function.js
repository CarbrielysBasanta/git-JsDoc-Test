/**
 * Add two numbers
 * @param {Number} n1 first params
 * @param {Number} n2 second number
 * @returns {Number} Total result
 */

function add (n1, n2){
    return n1 + n2;
}

/**
 * 
 * @param {Number} n1 first
 * @param {Number} n2 secodn
 * @returns {String}
 */

const addTwo = (n1, n2) => `The result is ${n1 + n2}`